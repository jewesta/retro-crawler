/**
 * Small shared value contracts and implementation utilities used by the core.
 *
 * <p>
 * Identifier, descriptor, attribute, serialization, reflection, hashing, and
 * path helpers live here when they do not own archive or Gear semantics. Most
 * utility classes support framework implementation; consumers should prefer the
 * domain APIs in the parent, archive, and Gear packages where available.
 */
package com.retrocrawler.core.util;

This is a mock local maven repository for dependencies that are
not currently available via maven central or which would require
the user to supply login credentials (which would be cumbersome).
See pom.xml for how it is used. Maven essentially treats it like
any other repository.